import type { Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        custom: ['Magic', 'sans-serif'],
      },
    },
  },
  plugins: [],
}

export default config
